# FastAPI Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Redis
redis==5.0.1
hiredis==2.3.2

# Celery
celery==5.3.6

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0
slowapi==0.1.9

# Data Processing
pandas==2.1.4
numpy==1.26.3

# FinLab Integration
finlab==1.5.4

# Shioaji Integration (永豐證券 API)
shioaji==1.2.9

# Technical Analysis
TA-Lib  # 不指定版本，讓 pip 自動選擇

# Backtesting
backtrader  # 不指定版本

# Qlib - Quantitative Investment Platform (Microsoft)
pyqlib>=0.9.0  # 正確的套件名是 pyqlib，不是 qlib
torch>=2.0.0  # Required for Qlib ML models
tqdm>=4.65.0  # Progress bars for Qlib
joblib>=1.3.0  # Parallel processing

# RD-Agent - Automated R&D Framework (Microsoft Research)
rdagent>=0.4.0  # Automated factor mining and strategy optimization
litellm>=1.80.7  # Multi-LLM backend support (required by RD-Agent, >=1.80.7 for rdagent 0.4.0)

# Machine Learning (Optional)
scikit-learn==1.4.0
lightgbm==4.2.0
xgboost==2.0.3

# AI Integration
openai>=2.9.0  # Upgraded to 2.9.0 for RD-Agent compatibility (requires openai.types.beta.threads support)
anthropic==0.8.1

# HTTP Client
httpx==0.26.0
aiohttp>=3.13.2  # Upgraded to 3.13.2 for litellm compatibility

# Utilities
python-dateutil==2.8.2
pytz==2024.1
click==8.1.7

# Monitoring & Logging
loguru==0.7.2
prometheus-client==0.19.0  # Prometheus metrics
celery-prometheus-exporter==1.10.0  # Celery metrics for Prometheus

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0

# Development
black==23.12.1
flake8==7.0.0
mypy==1.8.0
